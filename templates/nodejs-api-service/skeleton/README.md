# ${{ values.name }}

${{ values.description }}

Owner: ${{ values.owner }}

Scaffolded by the AWS IDP Backstage portal.

## Run locally

    npm start

Then hit http://localhost:8080 and http://localhost:8080/healthz
